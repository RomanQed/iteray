/**
 * Provides utilities for efficient and immutable iteration over object arrays with slicing support.
 * <p>
 * This module contains the core interfaces and classes for {@code Iterable<T>} and {@code Iterator<T>}
 * implementations backed by Java object arrays. It supports creating array views with start and end bounds,
 * allowing zero-copy iteration and functional-style transformations.
 */
module com.github.romanqed.jiter.array {
    // Exports
    exports com.github.romanqed.jiter.array;
}
